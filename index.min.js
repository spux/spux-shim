import 'https://unpkg.com/dataisland?module'

var viewScript

if (di && di.data && di.data.["@view"]) [
  viewScript = di.data.["@view"]
] else if (window && window.location && window.location.href) {
  viewScript = window.location.href.replace('.html', '').concat('.js')
}

import(viewScript)
